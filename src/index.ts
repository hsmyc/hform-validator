import Validator from "./validator";
/**
 * Validator class for input validation against schemas.
 *
 * @example
 * const validator = new Validator(schema);
 * const result = validator.validate(input);
 */
export { Validator };
